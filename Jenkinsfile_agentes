pipeline {
    agent { label 'agent1' } // Cambia por el label de cada agente.
    stages {
        stage('Check Git') {
            steps {
                script {
                    sh '''
                        echo "Comprobando git en el agente..."
                        git --version || echo "Git no está instalado en este agente."
                    '''
                }
            }
        }
    }
}
